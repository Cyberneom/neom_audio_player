name: neom_audio_player
description: Music Player Module for apps.
version: 1.2.0
homepage: https://github.com/Cyberneom/neom_music_player
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  neom_commons:
    git:
      url: git@github.com:Cyberneom/neom_commons.git
  neom_itemlists:
    git:
      url: git@github.com:Cyberneom/neom_itemlists.git

  audio_service: ^0.18.13
  just_audio: ^0.9.38

  metadata_god: ^0.5.2+1

  flip_card: ^0.7.0
  sliding_up_panel: ^2.0.0+1
  flutter_lyric: ^2.0.4+6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
