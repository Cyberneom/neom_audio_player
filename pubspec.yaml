name: neom_audio_player
description: Audio Player Module for apps.
version: 1.4.0
homepage: https://github.com/Cyberneom/neom_audio_player
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  neom_commons:
    git:
      url: git@github.com:Cyberneom/neom_commons.git
  neom_commerce:
    git:
      url: git@github.com:emmanuel-montoya/neom_commerce.git
  neom_media_player:
    git:
      url: https://github.com/Cyberneom/neom_media_player.git

  just_audio: ^0.9.46

  #DEPRECATED
  flip_card: ^0.7.0
  sliding_up_panel: ^2.0.0+1
  flutter_lyric: ^2.0.4+6

  #This would be needed when adding offline mode downloading audio.
  #metadata_god: ^1.0.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
